# Guidelines:
# 0) Download miniconda: wget https://repo.anaconda.com/miniconda/Miniconda3-latest-Linux-x86_64.sh
# 1) Execute installer: bash Miniconda3-latest-Linux-x86_64.sh
# 2) Initialize miniconda: ~/miniconda3/bin/conda init
# 3) Restart Shell: source ~/.bashrc
# 2) Create a conda environment in terminal: conda create -n S2 python=3.11
# 3) Activate the environment: conda activate S2
# 4) Install all requirements (from right path) in such environment: pip install -r requirements.txt
torch>=1.13.0
torchinfo>=1.7.0
numpy>=1.21.0
pandas>=1.3.0
scikit-learn>=1.0.0
matplotlib>=3.4.0
pyyaml>=6.0
tensorboard>=2.10.0
optuna>=4.4.0
adabelief-pytorch>=0.2.0
pytorch-ranger>=0.1.1
ruamel.yaml>=0.18.15
ruamel.yaml.clib>=0.2.8
optuna-dashboard

# Kronos forecasting stack
yfinance
statsmodels
tables

# AutoGluon (M2 ensemble classifier)
autogluon.tabular

# Online conformal prediction (SAOCP adaptive threshold)
online_conformal

# Feature selection (MDA/SHAP/LIME ranking)
shap
lime

# TabPFN
tabpfn
tabpfn-client

# TabICL (tabular in-context learning)
tabicl